//whenever you have been told to transform the array us map function
const input=[1,2,3,4,5,6];

function transform(i){
    return i * 2;
}
const ans = input.map(transform);
console.log(ans);


// so basically what we are doing is learning some new function 
// which is map function and its few line code